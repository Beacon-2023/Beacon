package com.BEACON.beacon.guideline.dao;

import com.BEACON.beacon.guideline.domain.CustomGuideLineEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CustomGuideLineRepository extends JpaRepository<CustomGuideLineEntity,Long> {
}
